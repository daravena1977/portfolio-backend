package com.app.miportfolio.repository;

import com.app.miportfolio.model.Laborales;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LaboralesRepository extends JpaRepository <Laborales, Long> {
    
}
